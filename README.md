This project validate a CPF and returns a response indicating if it's valid or not.
This is specifically prepared to be deploy on Azure functions.

Call example:
/api/CpfValidator?cpf=312.397.050-77

Response example:
{
  "isValidCpf": true
}
